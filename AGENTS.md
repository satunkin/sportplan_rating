# Project Agent Rules

Before doing substantial work in this repository:
1. Read [PROJECT_STATUS.md](/Users/satunkin/Codex_projects/rating/PROJECT_STATUS.md).
2. Treat `PROJECT_STATUS.md` as the primary short project memory.
3. If current work changes project reality, update `PROJECT_STATUS.md` before finishing.

Usage rules for `PROJECT_STATUS.md`:
- Keep only the current truth.
- Rewrite stale sections instead of appending conflicting status.
- Preserve the section structure unless there is a strong reason to change it.
- Keep it compact enough to be read at the start of a new chat.

If `PROJECT_STATUS.md` and code disagree:
- trust the code for implementation truth;
- then update `PROJECT_STATUS.md` to match.
